export const isDevMode: boolean = process.env.NODE_ENV === "development" || false;
export const isTestApiMode: boolean = process.env.NODE_ENV === "apitesting" || false;
export const isProdMode: boolean = process.env.NODE_ENV === "production" || false;
